{
  "name": "react-force-directed",
  "version": "1.0.0",
  "description": "React components for drawing force directed graphs using `d3-force`. Gracefully handles graph being updated by new objects representing the same graph via a `mergeGraph` helper.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && babel src/ -d dist/",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "@babel/preset-react": "^7.0.0",
    "babelify": "^10.0.0",
    "d3": "^5.9.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-hyperscript": "^3.2.0",
    "react-sizeme": "^2.5.2"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.4.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kumavis/react-force-directed.git"
  },
  "bugs": {
    "url": "https://github.com/kumavis/react-force-directed/issues"
  },
  "homepage": "https://github.com/kumavis/react-force-directed#readme"
}
